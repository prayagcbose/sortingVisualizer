import React from "react";

function Animate() {
	let outer = document.getElementsByClassName("outer");
	let inner = outer[0];
	console.log(outer);
	console.log(inner);
	return <div></div>;
}

export default Animate;
